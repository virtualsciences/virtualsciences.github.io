[buildout]
parts = instance
extends =
    default-settings.cfg
    parts.cfg
    hotfix.cfg
    http://dist.plone.org/release/${settings:version}/versions.cfg
find-links =
    http://dist.plone.org/release/${settings:version}
    http://dist.plone.org/thirdparty
versions = versions
eggs =
    ${hotfix:eggs}
develop =
    ${settings:develop}
